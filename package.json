{
  "name": "owlsweb",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build"
  },
  "vercel-build": "npm ci && npm run build",
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
